/** Standard left gutter for CLI + generator messages. */
export const INDENT = '  ';

/** Width of section rules under section titles. */
export const RULER_WIDTH = 48;

/** Column width for label/value alignment in previews. */
export const LABEL_COL = 12;
