import chalk from 'chalk';

/** CLI palette: primary orange, secondary cyan. */
export const palette = {
  primary: chalk.hex('#ff8c42'),
  secondary: chalk.cyan,
  success: chalk.green,
  warning: chalk.yellow,
  error: chalk.red,
  muted: chalk.dim,
};
